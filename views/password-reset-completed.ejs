<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Password reset completed</title>
    <link
      href="https://fonts.googleapis.com/css2?family=Open+Sans:wght@400;600;700&display=swap"
      rel="stylesheet"
    />

    <style>
      /* common css */
      * {
        margin: 0;
        padding: 0;
      }
      body {
        overflow-x: hidden;
        overflow-y: auto;
        background: #f3f3f3;
        font-family: "Open Sans", sans-serif;
      }
      .template-container {
        margin: 50px auto 25px auto;
        background-color: #ffff;
        width: 600px;
        border-radius: 10px;
        padding-bottom: 20px;
        border: 1px solid #e6e6e6;
      }
      /* header css */
      .template-header {
        text-align: center;
        border-bottom: 1px solid #e6e6e6;
        padding: 20px;
      }
      .logo-img {
        width: 180px;
        height: 50px;
      }
      /* header css */

      /* footer css */
      .template-footer {
        text-align: center;
        display: block;
      }
      .template-footer-heading {
        font-weight: 700;
        font-size: 20px;
        background: linear-gradient(90deg, #294393, #04a4e6 100%);
        -webkit-background-clip: text;
        background-clip: text;
        -webkit-text-fill-color: transparent;
      }
      .template-footer-description {
        font-size: 14px;
        font-weight: 400;
        color: #4c4c4c;
        margin-top: 8px;
        margin-bottom: 8px;
      }
      .template-footer-social-medias {
        text-align: center;
      }
      .template-footer-social-media-link {
        text-decoration: none;
        color: white;
        border-radius: 50%;
        width: 26px;
        height: 26px;
        display: inline-block;
        background: linear-gradient(180deg, #01904d 0%, #0ce47f 100%);
        position: relative;
      }
      .template-footer-social-media-icon {
        position: absolute;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
      }
      /* footer css */

      /* common css */

      /* common styles ended */
      .template-body {
        padding: 35px 30px;
      }
      .template-body-header {
        text-align: center;
      }
      .template-body-header h1 {
        font-size: 24px;
        font-weight: 700;
        color: #4c4c4c;
        margin: 5px 0px;
      }
      .template-body-header p {
        font-size: 16px;
        font-weight: 400;
        color: #4c4c4c;
      }
      .template-body-main {
        margin: 50px auto;
        position: relative;
      }

      #access-now-btn {
        position: absolute;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        background: linear-gradient(90deg, #fb8e48, #cb380f 100%);
        border-radius: 50px;
        padding: 10px 20px;
        color: #ffff;
        font-size: 16px;
        font-weight: 600;
        border: none;
        cursor: pointer;
        text-decoration: none;
        text-align: center;
      }

      @media screen and (max-width: 600px) {
        .template-container {
          padding-bottom: 15px;
          width: 100%;
        }
        /* header media query css start */
        .template-header {
          padding: 8px;
        }
        .logo-img {
          width: 130px;
          height: 40px;
        }
        /* header media query css end */
        /* footer media query css start */
        .template-footer-heading {
          font-weight: 600;
          font-size: 16px;
        }
        .template-footer-description {
          font-size: 12px;
          margin-top: 3px;
          margin-bottom: 3px;
        }
        .template-footer-social-media-link {
          width: 20px;
          height: 20px;
        }
        .template-footer-social-media-icon {
          width: 14px;
        }
        /* footer media query css end */

        .template-body {
          padding: 25px 20px;
        }
        .template-body-header h1 {
          font-size: 20px;
          font-weight: 600px;
          margin: 4px 0px;
        }
        .template-body-header p {
          font-size: 14px;
        }
        #access-now-btn {
          font-size: 14px;
          padding: 8px 16px;
          font-weight: 500;
        }
      }
    </style>
  </head>
  <body>
    <div class="template-container">
      <%- include('./components/header.ejs',{logoUrl}) %>

      <div class="template-body">
        <div class="template-body-header">
          <%- include('./components/heros/PassResetCompletedSvg.ejs') %>
          <h1>Password Reset Completed</h1>
          <p>
            Your password has been successfully reset. You can now log in with
            your new password. If you did not request this change, please
            contact support immediately.
          </p>
        </div>
        <div class="template-body-main">
          <a target="_blank" href="<%= accessUrl %>" id="access-now-btn">
            Access Now
          </a>
        </div>
      </div>
      <%- include('./components/footer.ejs') %>
    </div>
  </body>
</html>
